import styled from "@emotion/styled";
import { AccordionDetails, Box } from "@mui/material";

export const StyledBox = styled(Box)`
  margin-top: 1rem;
`

export const StyledSubsection = styled(AccordionDetails)`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: top;
  margin-top: 1rem;
`